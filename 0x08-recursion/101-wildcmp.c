#include "main.h"

/**
 * wildcmp - compares two strings to check
 * if they can be considered identical
 *
 * @s1: first string
 * @s2: first string
 *
 * Return: 1 if strings are considered identical
 * otherwise return 0
 */

int wildcmp(char *s1, char *s2)
{
	/* temp code to be replaced */
	if (s1 == s2)
	{
		return (1);
	}
	return (0);
}
