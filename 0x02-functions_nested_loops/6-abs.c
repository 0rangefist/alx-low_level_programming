#include "main.h"

/**
 * _abs - prints the absolute val of a number
 * * @n: character to check
 * Return: absolute val of a number
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
